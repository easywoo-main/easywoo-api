const SALT_ROUND = 10;
const MAX_FILE_SIZE = 1024 * 1024 * 10; // 10MB
const ALLOWED_FILE_TYPES = ["image/jpeg", "image/png", "application/pdf", "audio/mpeg", "video/mp4"];
export { SALT_ROUND, MAX_FILE_SIZE, ALLOWED_FILE_TYPES };
